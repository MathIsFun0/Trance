--these are all of the defaults, which can be modified if things don't look good
--I am still not sure why this renders slightly differently from vanila
return {
    render_scale = 10, --font resolution (also affects scale)
    TEXT_HEIGHT_SCALE = 0.83, --line spacing
    TEXT_OFFSET = {x=10,y=-20}, --offset (change if text is not centered)
    FONTSCALE = 0.11, --font size
    squish = 1, --stretches/shrinks text (NOT DynaText)
    DESCSCALE = 1 --scaling used by descriptions? still not exactly sure what this changes
}